# Instalar workflows

Mova os arquivos desta pasta para `.github/workflows/`:

```
cp pending-workflows/init-state-branch.yml .github/workflows/
cp pending-workflows/agent-release.yml .github/workflows/
```

Ou use o Claude Code:
> "Mova os arquivos de pending-workflows/ para .github/workflows/"
