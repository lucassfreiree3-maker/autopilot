services:
  psc-automacao-controller:
    build:
      context: .
    network_mode: host
    environment:
      - PORT=3000
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped