# Prompt: Autopilot Self-Check

Validate the persistent BBDevOpsAutopilot installation end to end. Confirm discovery files, manifest, token, canonical controller clone on `main`, deploy target branch `cloud/staging`, prompt library, skill availability, wrappers, environment variables, and release-state persistence. Report objective failures and repair only what is safe to repair automatically.
